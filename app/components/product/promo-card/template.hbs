
      {{#paper-card as |card|}}
        {{card.image src=product.imageUrl alt="Unit" title="Washed Out"}}
        {{#card.title as |title|}}
          {{#title.text as |text|}}
            {{#text.headline}}{{product.name}}{{/text.headline}}
          {{/title.text}}

        {{/card.title}}

    
        {{#card.actions class="layout-row layout-align-start-center" }}
          {{#paper-button}}Buy Now{{/paper-button}}

          {{#link-to 'products.product' product}}
                  {{#paper-button }}Read More{{/paper-button}}
          {{/link-to}}

          <div >

          <h3 class="price1" >     €{{product.price}}</h3>

          </div>

        {{/card.actions}}
      {{/paper-card}}


{{yield}}
